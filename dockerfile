# Базовый образ Python (можно указать конкретную версию, например python:3.9)
FROM python:3.12

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование зависимостей проекта
COPY requirements.txt .

# Установка зависимостей
RUN pip install --no-cache-dir -r requirements.txt

# Копирование всего проекта в контейнер
COPY . .

# Команда для запуска приложения (измените на вашу)
CMD ["python", "main.py"]